#pragma once

// Forward declarations
class Map;
class Camera;

// Screen dimensions
const int SCREEN_WIDTH = 950;  // Phải khớp với camera viewport
const int SCREEN_HEIGHT = 640; // Phải khớp với camera viewport

// Game physics
const float BULLET_SPEED = 400.0f;
const float SHOOT_COOLDOWN = 0.2f;